// Page wrapper for the account registration flow. Delegates all UI and logic to RegisterForm.
import RegisterForm from '../components/auth/RegisterForm';

export default function RegisterPage() {
  return <RegisterForm />;
}
